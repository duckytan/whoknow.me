# 笑率闸门执行包 · C2 口径对齐 + 跑门前准备清单（PREP-ONLY）

> **Task ID**：T-P1-GATE-01（收口/定稿复核）
> **编制人**：严守真（quality-lead）
> **日期**：2026-07-31
> **性质**：**准备文档，非改主包**。本文件不修改 `docs/qa/LAUGH-GATE-KIT-2026-07-31.md` 正文一个字。
> **前提**：C2 口径变更**尚未获锡哥批准**（汇编报告 §11 C2 明载「此变更需锡哥拍板后方可生效」）。本清单在锡哥拍板前仅作对齐准备，任何一条落地都需锡哥先批。

---

## 0. 一句话结论（先看这段）

**任务原设想「当前是旧口径 (a)，需准备切换到 C2」——方向反了。**
实际情况：**主包 `LAUGH-GATE-KIT` 正文已经全盘采用 C2 的 L0–L4 口径 (b)**，而 C2 本身还没批。
所以真正需要准备的不是「切到 C2」，而是**两条分支都要备好**：

- **分支 A（锡哥批 C2）**：主包几乎不用动，只需把 3 处「待确认」标记翻成「已批准」。**改动极小。**
- **分支 B（锡哥驳 C2）**：主包要**回退到旧口径 (a)**，这是**近乎重写**——因为整包的指标/阈值/记录表/归因法全建在 L0–L4 之上。**代价很大，且 07-26「中位 4.0」基线本就不可用（自评产生）。**

---

## 1. C2 口径现状裁定（带证据行号，不凭印象）

### 1.1 裁定结论

**当前 `LAUGH-GATE-KIT-2026-07-31.md` 使用的是 (b) —— C2 提议的「主持人观察 L0–L4、以 L2 出声笑划线」。不是 (a) 旧口径。**

### 1.2 证据（主包行号）

| 证据 | 位置 | 原文摘录 |
|---|---|---|
| L0–L4 分级定义表 | 主包 §1.1 **行 35–45** | 「**不用自评分，用外显反应分级。** 主持人在旁边观察并记录」+ L0–L4 五级判据表 |
| L2 划线 | 主包 §1.1 **行 45** | 「**为什么以 L2 划线**：L1 有礼貌性微笑污染……L2 出声笑是生理反应，装不出来」 |
| 指标全建在 L2+ | 主包 §1.2 **行 52–58** | LR=「达到 L2 及以上的节点数 ÷ 175」；NER/SR/L4C 均以 L2/L3/L4 定义 |
| 阈值表用 L 级 | 主包 §1.3 **行 62–68** | 五项阈值全部以 L2/L3/L4 表述，无任何「1–5 分」字样 |
| 差异表逐条自陈已改 | 主包 §1.5 **行 88** | 「原定义：受测者对每单打 1–5 分自评 → **本包改为：主持人记录 L0–L4 外显反应**」 |
| 明确标注「待锡哥确认」 | 主包 §1.5 **行 95** | 「尤其第 1 条，它推翻了 07-26 的基线数字……跑之前说，跑完再改口径就是耍赖」 |
| 记录表用 L 级 | 主包 §4.0 **行 218–231**、§4.1 **行 246–255** | 反应等级卡与表 A 逐节点填「L____」，无「1–5 分」列 |
| 需拍板项复述 | 主包 §9.3 **行 680** | 「§1.5 的 6 条差异是否认可——尤其第 1 条（不用自评分，改主持人观察）」 |

### 1.3 旧口径 (a) 的原文位置（供分支 B 回退参照）

`docs/PLAYTEST-CHECKLIST-2026-07-25.md` §H1：

| 项 | 位置 | 原文 |
|---|---|---|
| 方法（1–5 分自评） | §H1 **行 24** | 「每单对『四阶段段子时间线』打 **1–5 分**（5=笑出声，1=无感甚至不适）」 |
| 口径（中位/占比） | §H1 **行 26–28** | 「中位分 **≥ 3.5** 且 **≥ 60% 的订单 ≥ 4 分** → 过；中位 **3.0–3.4** → CONCERNS；中位 **< 3.0** → FAIL」 |
| 记录列 | §H1 **行 29** | 「`docs/playtest/<日期>-results.csv`（tester, 单号, 分支id, 评分, 备注）」 |

### 1.4 治理提示（重要）

主包在 C2 未批的情况下**已内嵌 C2**。这本身不是错误（严守真在 §1.5 行 95、§9.3 行 680 已如实标注「待确认」，属于「先按专业判断写好、请锡哥拍板」的正当做法），但它意味着：

> **真正的风险是反向的**——不是「忘了切到 C2」，而是「若锡哥驳回 C2，主包要大改」。
> 因此**必须在跑门前拿到锡哥对 C2 的明确批复**，否则一旦跑完再驳，整批数据的判定标尺失效（主包 §1.5 行 95 原话：「跑完再改口径就是耍赖」）。

---

## 2. 双分支改动清单（勿应用，等锡哥批）

### 2.1 分支 A · 锡哥批准 C2（推荐结果，改动极小）

主包内容**无需实质修改**，只需把「待确认」措辞更新为「已批准」，避免执行者误以为口径还悬着：

| # | 主包位置 | 现状措辞 | 批准后改为 |
|---|---|---|---|
| A1 | §1.5 行 95（差异表下方引注） | 「**待锡哥确认**：以上 6 条差异均为我的专业判断……」 | 「**锡哥已确认（2026-07-31）**：6 条差异生效，其中第 1 条（改主持人观察）为本轮判定基准」 |
| A2 | 头部行 7「状态」 | 「待锡哥确认阈值后即可开跑」 | 「口径与阈值已确认，满足 §9.1 前置即可开跑」 |
| A3 | §9.3 行 678–682（需拍板三件事） | 第 1 项「§1.5 的 6 条差异是否认可」 | 标注「✅ 已认可」，保留第 2、3 项（分组方案 / 前置修复范围）待办 |

> 分支 A 不动任何指标、阈值、公式、记录表——因为它们本来就是按 C2 建的。

### 2.2 分支 B · 锡哥驳回 C2（回退到旧口径 (a)，近乎重写）

若锡哥坚持用旧口径（受测者 1–5 分自评），主包需**系统性回退**。以下是逐段影响清单——**注意：这是重写级工作，不是改几个数字**：

| # | 主包位置 | 需改什么 | 严重度 |
|---|---|---|---|
| B1 | §1.1 行 33–45 | 删除 L0–L4 分级表与「L2 划线」全段，替换为「受测者每单 1–5 分自评」说明 | 高（核心定义推翻） |
| B2 | §1.2 行 47–58 | LR/NER/SR/L4C/TTF 全部重定义——自评分制下没有「节点级 L2 计数」，须回到「单级 1–5 分」，节点级诊断能力**丢失** | 高 |
| B3 | §1.3 行 60–68 | 五项阈值作废，换回 §H1 的「中位 ≥3.5 且 ≥60% 单 ≥4 分」单一口径 | 高 |
| B4 | §1.4 行 76–82 | 阈值推导论证（35%/90%/12% 的换算）整段作废 | 中 |
| B5 | §3.3 行 178–190 | 7 节点观察法的「记 L 级」列改回「整单印象打分」，N6 四格记法失去意义 | 高 |
| B6 | §4.0–§4.2 行 218–318 | 反应等级卡、表 A 逐节点 L 级、CSV 的 `reaction_level` 列全部改回「1–5 分/单」 | 高 |
| B7 | §5.1 行 341–361 | GD（爆点密度，基于 ≥L1）失去 L 级基础，须重定义或删除 | 中 |
| B8 | §5.2 行 363–413 | RF-A（基于 N6 四格 ≥L1）失去基础，须改用自评分差值 | 中 |
| B9 | §6.4 行 515–536 | LR_core / TR_shell 节点分离指标依赖 L 级，须重算 | 中 |
| B10 | §8 行 613–650 | 判定汇总表全部字段回退 | 中 |
| B11 | 全包「07-26 中位 4.0」相关 | **即使回退到 (a)，07-26「中位 4.0」仍不可作基线**——它是主理人自评产生的（汇编 §11 C2、PLAYTEST-CHECKLIST §H1 行 109），方法上不可比。回退不解决基线问题，只是把礼貌性通胀风险带回来 | 高（决策警示） |

> **分支 B 的专业意见（供锡哥权衡，非替锡哥决定）**：
> 回退到 (a) 会同时(1) 丢掉节点级诊断力（无法定位「哪个节点死了」）(2) 重新引入自评礼貌性通胀 (3) 让 07-26「中位 4.0」这个不可用基线看起来「可比」而实际不可比。
> **建议倾向批准 C2**；若锡哥对 L0–L4 某处判据有异议，优先「微调判据」而非「整体退回自评」。

---

## 3. 其他跑门前准备项（非 C2，独立于口径决策）

### 3.1 硬前置：§9.1 视觉真机复核（跑门前必须做一次）

- **状态**：主包 §9.1 要求 **D3 必须修、D1/D6 强烈建议修**。经核 `P1-FIX-2026-07-31.md`：
  - D3（首页自取死锁）**已修**（§1 D3 行 82–98），并在 §7.1 补修了 D3 同族的「自取高亮与内容不符」新穿帮。
  - D1（67 折）**已修**为「低至 6.7 折」（§1 D1 行 34–54，全量 5 角标比对通过）。
  - D6（原生 alert×3 + 漏报 confirm）**已修**为 app 内提示（§1 D6 行 102–120）。
  - 三绿：`npm test` 106/106、`vue-tsc` 0 错、`vite build` 成功。
- **缺口**：P1-FIX 行 290 明载工程侧**「没有浏览器实测环境，只做了代码级与数据级验证」**。即 D3/D1/D6 的**真机视觉穿帮未经实测确认**。
- **准备动作（跑门前，用即将用于跑门的那台已加主屏手机）**：
  1. 首页「首页/自取」来回切，确认高亮跟随、无死锁、无「高亮跳了但内容没跳」。
  2. 店铺页 5 个折扣角标确认显示「低至 X.X 折」，无「67 折」。
  3. 订单页「不支持报销」、设置页「清空数据」确认弹的是 app 内提示而非系统框。
  - 任一处仍穿帮 → **修好再开跑**，否则会在 N1/N3 观察节点当场废掉一个 tester 的数据。

### 3.2 B 版裸段子纸条文本（§6.1 方法一所需，主包只说「准备」未给文本）

主包 §6.1 要求准备「B 版裸段子纸条」，但未给出具体四句。以下**直接从 `src/engine/sliceDrama.ts` 推演得出**（确定性纯函数，逐字可复现），可直接抄到白纸：

**⚠️ 措辞歧义待澄清**：主包 §6.1 行 450（被试内方案）写「给他看**第 4 单那组他还没见过的组合**的 B 版纸条」——但第 4 单（ICU 病房 + 老板辛苦了）是他**在 A 版跑过的**，不是「没见过」。这里自相矛盾。两种读法都给出文本，请锡哥/主持人跑前定一个：

**读法一（照字面 = 第 4 单组合 s01 / ICU 病房 / 老板辛苦了）**——bossMood=90，warm 带：
```
接单(老板)：ICU 病房？我轻着点做，您安心养着。老板辛苦了？嘿，被你这么一夸，更得好好伺候了。
出餐(老板)：（小声）您这单我得快手快脚又轻手轻脚地做，汤别洒了。（心里一暖）您这句话我记下了，多放份量。
配送(骑手)：ICU 这单我开得稳稳的，您别急，门帮您留着。
送达(老板)：趁热趁软乎……您慢用，好好休息。（辛苦啥，您爱吃就行）
```

**读法二（推荐 = 用一个他确实没跑过的组合 s01 / 公司 / 表演才艺）**——bossMood=55，neutral 带，与他跑过的组合都不同，更符合「破坏性对照」本意：
```
接单(老板)：公司单？行，你们打工人互相折磨呗。表演才艺？成，今儿给你来一段。
出餐(老板)：（叹气）公司单我也就应付下，别指望多用心。先去换身行头，您稍等，演出级出餐。
配送(骑手)：公司楼我天天跑，电梯挤死，但准时给您放前台了。
送达(老板)：拿好，回工位趁热扒两口，别被老板抓包。谢幕鞠躬，下次点个「encore」呗。
```

> 严守真建议用**读法二**：破坏性对照要的是「同等新鲜度下 A 壳 vs B 纸」的差值，用他见过的组合会低估外壳作用。

### 3.3 主包 §3.4 一处算术笔误（不影响跑门，建议顺手更正）

- 主包 §3.4 **行 201**：第 4 单标注「bossMood = 50+20+10 = **80**，落 warm 带」。
- 经核 `sliceDrama.ts`：`ADDRESS_OFFSETS.icu = +20`、`REMARK_OFFSETS.boss_thx = +20`（行 72–84），实际 **50+20+20 = 90**。笔误把 boss_thx 的 `gentleMood=10`（行 101，出餐阶段回弹值）误当成了接单偏移。
- **影响**：无。90 与 80 都落 warm 带（≥60），演出文案与设计意图完全不变。仅数字标注需从 `80` 改 `90`。
- 处置：属主包正文，本轮**不改**（等 C2 一并处理时顺手修）。

### 3.4 数据回收模板落盘

- 主包 §4.3 行 322 建议把 CSV 表头存为 `docs/playtest/laugh-gate-scorecard-template.csv`，但该文件**尚未创建**。
- 主包 §4.3 行 320 引用的 `scripts/playtest-aggregate.ts` 为「后续改造对接」——**本轮无自动聚合**，判定汇总（§8）需**人工从纸质表 A/B 誊算**。
- 准备动作：跑门前创建该 CSV 模板文件（表头见主包行 325），并指定一名誊算人；或接受纯手工统计（5 人 ×25 单，量不大，手工可行）。

### 3.5 录制机制（TTF/TTB 计时精度）

- 主包 §5.1 行 361 / §6.2 行 486 的 TTF、TTB 要求「从主屏图标被点下那一刻起算」的秒级时间戳。
- **仅靠录音**难以精确捕捉「屏幕被点下」的瞬间。建议：**屏幕录制 + 外部录音**同开，或约定主持人在受测者点图标同时口播「开始」作为音频锚点。这决定 TTF（门 E 的 3 秒线）能不能量准。

### 3.6 tester 招募话术（防剧透）

- 主包 §2.2 给了 tester 硬条件、§3.2 给了到场后的开场白，但**未给招募阶段话术**（怎么把人约来又不暴露「这是个游戏」）。
- 建议统一招募口径（勿说「测游戏/测好不好笑」，只说「帮我看个外卖点单的东西，5 分钟」），并叮嘱转介绍人不得剧透。此项属主持人执行细节，非阻塞，跑前对齐即可。

---

## 4. 跑门前 Checklist（关门项汇总，供锡哥/主持人逐条勾）

```
【口径门 · 必须先关】
□ 锡哥对 C2（L0–L4 口径）明确批复 —— 批→走分支A(A1-A3)；驳→走分支B(B1-B11，重写)
  ⚠ 未拿到批复不得开跑（跑完再改口径 = 数据判定标尺失效）

【硬前置 · §9.1】
□ 真机视觉复核 D3/首页自取切换（代码已修，真机未验）
□ 真机视觉复核 D1 折扣角标「低至X.X折」
□ 真机视觉复核 D6 app内提示（非系统弹窗）
□ 手机已「添加到主屏」从图标启动（去地址栏）

【物料】
□ 打印 主包§4.1 表A ×5、§4.2 表B ×5、§4.0 等级卡 ×1
□ B版裸段子纸条（用本文§3.2 读法二文本，或锡哥定读法一）
□ CSV模板落盘 或 指定手工誊算人
□ 屏幕录制+录音（TTF/TTB锚点）已就绪、已征得同意

【人与场】
□ 5人符合硬条件（没玩过+近30天≥8次外卖+非相关+含1冷面型+2年龄段）
□ 一对一无围观、一天内跑完
□ 主持人纪律：不笑/不解释/不指路/不剧透
```

---

## 5. 自我声明

- 本文件**未修改** `LAUGH-GATE-KIT-2026-07-31.md` 及任何 `src/`、任何数据文件，**未执行 git commit/push**。
- 本文件为 **PREP-ONLY**：所有「分支 A/B 改动」均待锡哥对 C2 拍板后方可应用；应用动作由主理人统一调度。
- 引擎口径核对基于 `src/engine/sliceDrama.ts`（commit 线上版）实读，非记忆。

**编制**：严守真 · quality-lead · 2026-07-31
