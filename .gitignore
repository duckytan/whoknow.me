# ── 依赖 ──
node_modules/
.pnpm-store/
.yarn/

# ── 构建产物 ──
dist/
build/
*.tsbuildinfo
# 例外：胡闹外卖的预构建产物收进单一项目文件夹 whoknow-waimai/dist/，需提交以支持 Vercel 静态部署（/waimai → whoknow-waimai/dist）
!whoknow-waimai/dist/

# ── 环境变量 ──
.env
.env.local
.env.*.local

# ── 系统文件 ──
.DS_Store
Thumbs.db
*.swp
*.swo

# ── IDE ──
.vscode/
.idea/
*.sublime-*

# ── 临时文件 ──
*.log
.tmp/
temp/

# ── 旧项目引用源码（不推） ──
opensource-refs/
research-raw/

# ── agent 本地记忆（非项目文件，不推） ──
.workbuddy/
