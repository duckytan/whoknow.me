# 三司会审报告 · whoknow-brain 方案讨论计划（DISCUSSION-PLAN.md）· 2026-07-26

**任务 ID**：sanshi-20260726-001
**模式**：圆桌（核心架构 / 新铁律 design-lock / 高污染风险 → 三司并行隔离）
**主控**：游承峰（工作室主理人，任三司会审核心大脑）
**任务开始**：2026-07-26 20:20
**审查对象**：`whoknow-brain/docs/DISCUSSION-PLAN.md`（7 章 28 题 + design-lock gate）

---

## 任务本质卡片
- **表面需求**：三司会审审计这份大脑方案讨论计划。
- **真实需求**：验证"大脑如何从概念落到底层架构"的蓝图是否严谨、决策依赖链是否合理、有无重大遗漏、设计锁闸能否真防"边做边改"。
- **边界**：只审文档（方案设计质量）；不修改文档、不进 Phase 3 写码、不替用户拍板 D1–D5 决策。
- **负向清单**：不改 DISCUSION-PLAN.md / 不动 brain 代码契约 / 不替用户决策。

---

## 三司结论（带印证 · 6 方向）

### 朱熹（明辨决）— 主"对不对"：事实核验 + 实态核验 + 根因
- **核心结论**：计划"行（28 题之工）有余、知（与实态相穷）不足"。作为设计锁闸载体不够严谨，且部分建立在失真事实上。
- **实态核验关键发现**：
  - F9：`api-spec.md` 实为**运行时拓扑 B**（collector/cron/`/api/v1`/接管运营），与本文档推荐的静态拓扑 A 直接冲突。
  - F2：文档称 api-spec 为 **v2.2**，实为 **v2.1**（CONSTITUTION 标"部分"）。
  - F6：waimai "58 分支"实为 **96 个 id 字段**，无"58"出处。
  - F8：设计锁闸**无任何执行钩子**（`.githooks` 只查主观表述/提交格式；CONSTITUTION 无 design-lock 条款）。
- **根因**：计划未与仓库既有权威文档（api-spec / CONSTITUTION / INDEX）做"实态对齐"，且 CLOSED 缺客观判定准绳、锁闸无执行钩子。
- **确信度**：高。质量评分：完整性 4/5 · 证据链 2/5 · 可执行性 2/5。

### 六祖慧能（破妄决）— 主"信不信"：假设 / 信念前提穿透
- **核心结论**：条件性通过（**不通过作为 Phase 3 依据**）。在调和"拓扑 A vs api-spec B"立场前，本计划不可作锁闸闭合证据。
- **隐藏前提清单（HP）**：
  - **HP-1（0.85）**："DISCUSSION-PLAN 与所引 api-spec 是同一拓扑（静态 A）"——妄执；api-spec 实为运行时 B，是本文档在 Q1.1/Q1.2 否决的东西。破了 → Phase 3 实施者按 B 做出来的与锁死的 A 冲突，讨论全废。
  - **HP-2（0.80）**："两条铁律不可动摇且已被守住"——妄执；api-spec 的 collector 采玩家反馈/埋点、`/api/v1/track` 破"不持玩家数据"；0 成本实为 ¥10-15/月低运维非 0。
  - **HP-3（0.75）**："设计锁闸能防边做边改"——自性执；锁给人纪律+钩子，而文档自承人易犯；前提错时锁死的是错误方案。
  - **HP-4（0.70）**："锡哥要静态/0 控制感大脑"——妄执；api-spec 写"锡哥放手时 brain 接管运营"，要的是控制感。
  - **HP-5（0.65）**："AI 起草+锡哥抽检能持续产出够胡闹可控内容"——妄执；锡哥自承"只能偶尔抽样看"，审核带宽有限。

### 老子（五行诀）— 主"全不全 / 久不久"：维度平衡 + 长期 + 反脆弱
- **五行诊断**：**金太过、水不及**。设计锁闸/否决/硬清单（金）过旺，压制实施启动（木）→ 相乘"金乘木 = 设计瘫痪"；三司会审太重 → 木侮金"嫌重跳过评审"；文档"火过旺"（28 题势能）但"水（真机实态）枯绝"。
- **最该补 3 项**：
  1. 登记"拓扑 A 与 api-spec/B 冲突"并定义重写归属（地基裂缝，缺它一切讨论无法落地）。
  2. 补"Phase 3 之后轻量持续闸门"+ 把三司会审降为"重大变更才触发"（修复反效果）。
  3. 把已发生真问题（活数据红灯未洗 / 锡哥手动供给 / 只写不执行 / mood 字段冲突）从"举一反三未来语气"提升为 R11–R14 已发生风险登记。
- **颠倒诀（最可能失败）**：① 设计瘫痪（金乘木，全链冻死）；② 实施者按哪份文档（A 否决 B，但 api-spec 被宪法指为权威）；③ 锡哥审核瓶颈；④ fetch 带宽欠账。

---

### 网状印证表（6 方向 · 主控交叉对比）
> 并行隔离下 6 方向为主控读三司真实文本后的推测印证，非各司互评。

| 方向 | 印证结论 |
|---|---|
| 朱熹看 六祖 | 朱的"F9 拓扑冲突" = 慧能"HP-1"，事实层坐实了假设层 |
| 朱熹看 老子 | 朱的"锁闸无钩子(F8)" = 老子的"金太过、水不及"，可执行性弱 |
| 六祖看 朱熹 | 慧能"HP-3 锁死错方案" 补强了朱"根因：前提错则下游全歪" |
| 六祖看 老子 | 慧能"HP-4 锡哥要控制感" = 老子"木虚根（控制欲未满足）" |
| 老子看 朱熹 | 老子"金乘木设计瘫痪" = 朱"依赖链压在受挑战的 Q1.2=A 预设上" |
| 老子看 六祖 | 老子"第三项已发生真问题轻描" = 慧能"焦点4 原框架收敛过猛靠补丁" |

**差异自检**：三司从"事实 / 假设 / 五行"三个不同维度**独立收敛到同一根因**（拓扑 A vs api-spec B 冲突 + 锁闸空闸门），属强交叉印证，非机械雷同；角色分工（对不对/信不信/全不全）有效互补，未出现同维三审。

---

### 假设穿透综合（并行模式必填）
| 隐藏前提 / 妄执 | 戳破方 | 为何是妄执 / 风险 |
|---|---|---|
| HP-1 拓扑 A 与 api-spec B 一致 | 朱(F9)/慧(HP-1)/老(焦点1) | 文档锁死静态 A，所引 api-spec 实为运行时 B；Phase 3 必爆 A/B 之争 |
| HP-2 两铁律不可破 | 慧(HP-2)/老(焦点1) | api-spec 采玩家数据+服务端，铁律被自身依赖破 |
| HP-3 锁闸防边做边改 | 朱(根因)/慧(HP-3) | 无钩子=愿望；前提错时焊死错方案 |
| HP-4 锡哥要静态大脑 | 慧(HP-4)/老(木虚根) | api-spec 写"接管运营"，要控制感 |
| HP-5 人审带宽够 | 慧(HP-5)/老(焦点4) | 锡哥自承偶尔抽样，量产同质化风险 |

---

## 风险与建议

### 🔴 P0（必须解决，否则计划不能作为 Phase 3 依据）
1. **拓扑双重真相（地基裂缝）**：DISCUSSION-PLAN 推荐静态 A，但 `api-spec.md`（CONSTITUTION L2-C4 指为契约权威）实为运行时 B。二选一必须先在"讨论"层面拍死：
   - 若采 A：正式把 `api-spec.md` 重写/降级为静态管道版，消除双重真相；
   - 若保留 B：Q1.2 应改推荐 B，并显式说明如何不破 0 成本铁律。
   - **动作**：在 D0 之前新增 **Q0.0 拓扑立场对齐** 一题，作为一切的前置闸门。
2. **锁闸是愿望不是闸门**：`.githooks/pre-commit` 加最小校验——`whoknow-brain/**` 下新增源码改动若对应题未 `CLOSED` 或缺 ADR 则拦截；CONSTITUTION 补 design-lock 一行红线。

### 🟡 P1（建议补强）
3. **事实校准**：api-spec 标 v2.1（非 v2.2）；删除/更正"58 分支"为实测数；注明锁闸当前未 codify、无钩子。每条断言附仓库路径。
4. **CLOSED 客观判准**：每题"最终推荐"后补"闭合验收清单"（如 Q2.3 闭合需"Zod 产出+消费方 import 通过"），否则 CLOSED 沦为"聊过即勾"。
5. **铁律边界澄清**：把"0 成本"明确定义为"低运维 ≤¥15/月"、把"不持玩家数据"明确排除"匿名行为埋点"，或反之修订铁律——消除与 api-spec 的隐性冲突。
6. **已发生真问题提级**：活数据红灯未洗 / 锡哥手动供给 / 只写不执行 / mood 字段冲突，从"举一反三"提升为附录 B 的 R11–R14 已发生风险。

### 🟢 P2（长期）
7. 内容库增长/TTL 归档、LLM 换代质量漂移、701-PC 单机单点、多 app 治理膨胀——补进 R 表。
8. 锁闸只防"动工前"，补"Phase 3 后轻量持续闸门"（CI 自动硬清单 + ADR 被否决方案记录）。

---

## 落地四问（编排层附加轴）
1. **用户滤网**：本件为内部治理文档，不直接面向截图传播；但"锁闸能真防边做边改"正对应用户"大脑不能边做边改"的强诉求——审计发现锁闸当前是**空闸门**，与用户诉求背道而驰，须补。
2. **成本**：三司会审 token 成本已发生（本报告）；修复成本低（对齐拓扑 + 加钩子 + 校准事实，几乎零代码）。
3. **风险**：最大风险 = 拿错误前提（A 锁死）进 Phase 3 → 实施期爆发 A/B 冲突 → 前期讨论全废。缓解 = 先开 Q0.0。
4. **项目对齐**：与 CONSTITUTION 铁律/工程约定一致（审计正是为保铁律）；但发现 api-spec 与铁律冲突需调和，属必要纠偏。

---

## 综合采信建议（出口闸门）
- **建议**：**谨慎 / 条件性通过（不通过作为 Phase 3 依据）**
- **依据**：三司确信度均值高，结论强交叉印证；根因明确（拓扑 A 与 api-spec B 双重真相 + 锁闸无执行钩子）；文档 28 题框架本身有价值、可保留。
- **一句话理由**：这份计划"行（28 题之工）有余、知（与实态相穷）不足"——在**未先调和拓扑 A 与 api-spec B 的立场冲突、未把锁闸从愿望变闸门**之前，不可作为 design-lock 的闭合证据放行 Phase 3；框架保留，先补 Q0.0 拓扑对齐题 + 事实校准 + 落子钩子。

---

## 给用户的下一步（待拍板）
1. **是否采纳"先开 Q0.0 拓扑立场对齐题"**？这是所有讨论的前置闸门——先回答"我们到底要不要推翻 api-spec.md 的拓扑 B"，再谈 D1–D5。
2. 要不要我据此**回写** DISCUSSION-PLAN.md（新增 Q0.0 + 校准事实 + 把锁闸接上钩子）？——需你许可（本轮回审严守"只审不改"边界，未动原文件）。

> 三司会审结论：文档勤勉、结构可用，但地基有一道裂缝（拓扑双重真相）和一道空门（锁闸无钩子）。先补这两处，再谈"讨论透彻"。
